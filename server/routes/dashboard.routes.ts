/**
 * Modular dashboard router placeholder.
 * Authoritative metrics are served by production.routes GET /api/dashboard
 * (mounted at /api). Do not add a second conflicting metrics endpoint here.
 */
import { Router } from 'express';

const router = Router();

export default router;
