// Legacy path — re-exports the server-authoritative TaskCalendar (Step 5C).
// All calendar events are fetched from scheduled_activities (Asia/Dhaka, visibility-enforced).
export { default } from '../modules/auth/pages/TaskCalendar';
