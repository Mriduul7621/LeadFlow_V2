/**
 * Shared API placeholders. Dashboard metrics live in dashboardService
 * (GET /api/dashboard) — do not use mock totals here for production UI.
 */
export { dashboardService, type DashboardMetrics } from '../../dashboard/services/dashboardService';
